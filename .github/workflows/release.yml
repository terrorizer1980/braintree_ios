name: Build
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release"
        required: true
jobs:
  release:
    name: Release
    runs-on: macOS-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v1
      - name: Use Xcode 12.3
        run: sudo xcode-select -switch /Applications/Xcode_12.3.app
      - name: Install CocoaPod dependencies
        run: pod install
      - name: Update version in podspec
        run: sed -i '' 's/s\.version = .*/s\.version = \"'"${{ github.event.inputs.version }}"'\"/' Braintree.podspec
      - name: Show diff
        run: git diff
